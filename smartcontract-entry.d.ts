/**
 * Script-level contract construction: OP_PUSH_TX covenants, BIP-143 preimage
 * tooling, the interpreter-backed tester, and the template library.
 *
 * Same surface as `require("@smartledger/bsv").SmartContract`.
 */
import { SmartContract } from '@smartledger/bsv';
export = SmartContract;
